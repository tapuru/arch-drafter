import { VerificationLinkExpiredForm } from '@/features/auth';

export const VerificationLinkExpiredPage = () => {
  return (
    <div className="flex min-h-screen items-center justify-center p-4 primary-gradient">
      <VerificationLinkExpiredForm />
    </div>
  );
};
